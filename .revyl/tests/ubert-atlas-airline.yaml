# Revyl Test Definition

_meta:
    remote_id: 96bfed58-f59c-496a-9e6c-af5dfe5a1c06
    remote_version: 1
    local_version: 1
    last_synced_at: "2026-05-21T09:19:28-07:00"
    checksum: 8555955d70056f48a2cf0ffbb88261eb6133aefa1c81efc6cc068816cb82369b
test:
    metadata:
        name: ubert-atlas-airline
        platform: ios
        tags:
            - ethan
            - atlas
            - airline
    build:
        name: Ubert
    blocks:
        - type: validation
          step_type: validation
          step_description: The Splash screen is visible. Background is solid black and the white "UBERT" wordmark is shown center-screen.
        - type: manual
          step_type: wait
          step_description: "3"
        - type: validation
          step_type: validation
          step_description: The Home screen is visible. There are two top tabs labeled "Rides" and "Delivery", a search bar with placeholder "Where to?", and a bottom tab bar showing "Home", "Services", "Activity", "Account".
        - type: instructions
          step_type: instruction
          step_description: Tap the "Where to?" search bar on the Home screen.
        - type: validation
          step_type: validation
          step_description: The Search screen is visible. At the top there is a back arrow, an origin field showing the current location address with a green dot, and a focused destination input with placeholder "Where to?" and a black dot to its left. Below the inputs is a light grey "Airport pickup · Pick airline" chip with a flight icon on the left and a chevron on the right. Under that is a "Recent" section header and a list of recent place rows, followed by a "Suggestions" section.
        - type: instructions
          step_type: instruction
          step_description: Tap the "Airport pickup · Pick airline" chip below the search inputs.
        - type: validation
          step_type: validation
          step_description: The Airline picker screen is visible. The header title reads "Pick your airline" with a back chevron. Below the header is a grey search field with placeholder "Search airlines or IATA code", followed by the helper text "Your airline helps your driver pick the right terminal for drop-off." A vertical list of airlines is shown, each row with a colored square logo containing the IATA code, the airline name, a "Terminal T#" subtitle, and a radio button on the right. Visible airlines include "American Airlines" (AA, Terminal T8), "Delta Air Lines" (DL, Terminal T4), "United Airlines" (UA, Terminal T7), "JetBlue Airways" (B6, Terminal T5), and "Southwest Airlines" (WN, Terminal T1). A disabled black "Pick an airline" button is pinned at the bottom.
        - type: instructions
          step_type: instruction
          step_description: Tap the "Delta Air Lines" row in the airlines list.
        - type: validation
          step_type: validation
          step_description: The "Delta Air Lines" row now shows a filled black radio dot on the right indicating it is selected. The bottom button is now active black and reads "Confirm Delta Air Lines".
        - type: instructions
          step_type: instruction
          step_description: Tap the black "Confirm Delta Air Lines" button pinned at the bottom of the Airline picker screen.
        - type: validation
          step_type: validation
          step_description: The Search screen is visible again. The destination input with placeholder "Where to?" is shown along with the "Airport pickup · Pick airline" chip below it, and the "Recent" / "Suggestions" sections of places.
        - type: instructions
          step_type: instruction
          step_description: Tap the back arrow at the top-left of the Search screen to return to the Home screen.
        - type: validation
          step_type: validation
          step_description: The Home screen is visible again. The "Where to?" search bar, "Rides"/"Delivery" tabs, and bottom tab bar with "Home", "Services", "Activity", "Account" are all visible.
