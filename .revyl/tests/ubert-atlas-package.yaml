# Revyl Test Definition

_meta:
    remote_id: 12e6a835-405e-4e82-9a39-f55f7c0591c7
    remote_version: 1
    local_version: 1
    last_synced_at: "2026-05-20T20:59:02-07:00"
    checksum: 2b2b9f3ad81069ac6898bc2e96a8df1f291156f92ad474d087e2474945d711c6
test:
    metadata:
        name: ubert-atlas-package
        platform: ios
        tags:
            - ethan
            - atlas
            - package
    build:
        name: Ubert
    blocks:
        - type: validation
          step_type: validation
          step_description: The Splash screen is visible. Background is solid black and the white "UBERT" wordmark is shown center-screen.
        - type: manual
          step_type: wait
          step_description: "3"
        - type: validation
          step_type: validation
          step_description: The Home screen is visible. There are two top tabs labeled "Rides" and "Delivery", a search bar with placeholder text "Where to?", and a bottom tab bar showing "Home", "Services", "Activity", "Account".
        - type: instructions
          step_type: instruction
          step_description: Tap the "Services" tab in the bottom tab bar to open the Services bottom sheet.
        - type: validation
          step_type: validation
          step_description: The Services bottom sheet is open with the title "Services" at the top. It shows a grid of service cards including "Ride", "Package", "Reserve", and "Rent" inside a modal sheet that overlays the Home screen.
        - type: instructions
          step_type: instruction
          step_description: Tap the "Package" service card in the Services bottom sheet.
        - type: validation
          step_type: validation
          step_description: The Package Details screen is visible. The header reads "Send a package" with a back chevron. The heading "What are you sending?" is shown above the subtitle "Pick the size that fits your package." A list of size cards is displayed labeled "Envelope" (Documents, small items), "Small" (Up to a shoebox), "Medium" (Up to a carry-on), and "Large" (Fits in a sedan trunk), with the "Small" card selected by default. Below is a "Description" heading and a multiline text area with placeholder "What's inside? (optional)". A black "Continue" button is pinned at the bottom.
        - type: instructions
          step_type: instruction
          step_description: Tap the "Medium" size card in the list so the selection moves from "Small" to "Medium".
        - type: instructions
          step_type: instruction
          step_description: Tap the description text area with placeholder "What's inside? (optional)" and type "Birthday gift for Alex".
        - type: instructions
          step_type: instruction
          step_description: Tap the black "Continue" button pinned at the bottom of the Package Details screen.
        - type: validation
          step_type: validation
          step_description: The Package Sender screen is visible. The header reads "Sender info" with a back chevron. The heading "Where should we pick it up?" is shown. Three labeled input fields are displayed in order, each with its uppercase label above the input - "PICKUP ADDRESS" pre-filled with "350 5th Ave, New York, NY", "SENDER NAME" pre-filled with "Ethan Zhou", and "SENDER PHONE" pre-filled with "+1 (555) 010-2233". A black "Next - recipient" button is pinned at the bottom.
        - type: instructions
          step_type: instruction
          step_description: Tap the "SENDER NAME" input field and replace its value with "Ethan Z".
        - type: instructions
          step_type: instruction
          step_description: Tap the "SENDER PHONE" input field and replace its value with "+1 555 010 0000".
        - type: instructions
          step_type: instruction
          step_description: Tap the black "Next - recipient" button pinned at the bottom of the Sender info screen.
        - type: validation
          step_type: validation
          step_description: The Package Recipient screen is visible. The header reads "Recipient info" with a back chevron. The heading "Where should we drop it off?" is shown. Three labeled input fields are displayed in order with uppercase labels - "DROP-OFF ADDRESS" pre-filled with "1 World Trade Center, New York, NY", "RECIPIENT NAME" pre-filled with "Alex Johnson", and "RECIPIENT PHONE" pre-filled with "+1 (555) 030-4455". A black "Review" button is pinned at the bottom.
        - type: instructions
          step_type: instruction
          step_description: Tap the "RECIPIENT NAME" input field and replace its value with "Alex J".
        - type: instructions
          step_type: instruction
          step_description: Tap the "DROP-OFF ADDRESS" input field and replace its value with "30 Rockefeller Plaza, New York, NY".
        - type: instructions
          step_type: instruction
          step_description: Tap the black "Review" button pinned at the bottom of the Recipient info screen.
        - type: validation
          step_type: validation
          step_description: The Package Confirm (Review package) screen is visible. The header reads "Review package" with a back chevron and a heading "Send a package" below it. Three sections are stacked in order - "PACKAGE" with rows for "Size" (Medium) and "Description" (Birthday gift for Alex), "PICKUP" with rows for "Address", "Sender", and "Phone", and "DROP-OFF" with rows for "Address", "Recipient", and "Phone". A highlighted fare card shows a truck icon, the label "Estimated fare" with subtitle "Includes delivery and handling", and the dollar amount "$22.50" on the right. A black "Send package - $22.50" button is pinned at the bottom.
        - type: instructions
          step_type: instruction
          step_description: Tap the black "Send package - $22.50" button pinned at the bottom of the Review package screen.
        - type: validation
          step_type: validation
          step_description: The Home screen is visible again with the "Rides"/"Delivery" tabs, the "Where to?" search bar, and the bottom tab bar showing "Home", "Services", "Activity", "Account". A success toast briefly appears reading "Package booked".
